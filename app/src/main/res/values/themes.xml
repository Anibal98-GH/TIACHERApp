<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TIACHER" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/degradado_background</item>

    </style>

    <style name="Theme.TIACHER" parent="Base.Theme.TIACHER" />
</resources>